# upantry-core

## Setup

Install dependencies:

```sh
brew install nvm
nvm install 20

cd client
npm i
```

- Run the dev server: `npm run dev`
- Open the URL displayed in the terminal

# upantry-be
